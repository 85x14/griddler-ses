require 'spec_helper'

describe Griddler::Ses do
  it 'has a version number' do
    expect(Griddler::Ses::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
